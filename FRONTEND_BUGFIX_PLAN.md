# 前端Bug修复计划

## 问题列表

### 1. 智能评价：完成后点击导航栏无反应
**问题**：评价结果显示后，再次点击导航栏"智能评价"，页面无反应
**原因**：路由监听逻辑有问题，当路径相同时不会触发重置
**解决方案**：修改 Evaluation.vue 的路由监听逻辑

### 2. 智能评价：缺少取消上传功能
**问题**：上传/处理过程中无法取消
**原因**：未实现取消功能
**解决方案**：添加取消按钮和取消逻辑

### 3. 智能评价：切换菜单导致进度中断
**问题**：评价过程中切换到其他页面再切回来，进度中断
**原因**：未保存处理状态，切换页面后丢失
**解决方案**：参考 SpellCheck 和 Formatting 的实现，添加状态保持逻辑

### 4. 错别字检查：两个关闭按钮
**问题**：进度条后有多余的关闭按钮
**原因**：代码中有重复的关闭按钮
**解决方案**：删除进度条后的关闭按钮，只保留一个

### 5. 错别字检查：切换菜单后保留数据 ✅
**问题**：检查完成后，切换菜单再回来，仍显示上次数据
**原因**：路由监听逻辑问题
**解决方案**：修改路由监听，只在处理中状态时恢复轮询
**状态**：已修复

### 6. 错别字检查：下载文档表格格式问题
**问题**：下载的文档中表格文字错版，未对齐，表格线位置不对
**原因**：后端降级处理时丢失了表格格式
**解决方案**：这是后端问题，需要改进降级处理逻辑

### 7. 模板排版：两个关闭按钮
**问题**：进度条后有多余的关闭按钮
**原因**：代码中有重复的关闭按钮
**解决方案**：删除进度条后的关闭按钮

### 8. 模板排版：切换菜单后保留数据 ✅
**问题**：完成后，切换菜单再回来，仍显示上次数据
**原因**：路由监听逻辑问题
**解决方案**：修改路由监听，只在处理中状态时恢复轮询
**状态**：已修复

### 9. 历史记录：筛选无需点击按钮
**问题**：选择类型和日期后，不点击"筛选"按钮就直接返回结果
**原因**：使用了 computed，自动响应变化
**解决方案**：改为手动筛选模式，需要点击按钮才筛选

### 10. 导航栏用户头像无功能 ✅
**问题**：右侧用户头像图标有交互但无功能
**原因**：未实现用户菜单功能
**解决方案**：移除交互效果（cursor: pointer 和 hover 动画），仅作展示
**状态**：已修复

## 修复优先级

1. 高优先级（影响功能）：1, 3, 5, 8, 9
2. 中优先级（用户体验）：2, 4, 7, 10
3. 低优先级（后端问题）：6
