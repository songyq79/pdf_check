这份更新后的技术开发文档（V2.0版本）整合了**分层漏斗模型**、**混合检索架构**以及**Lambda实时数据处理**，旨在解决亿级文档规模下的查重精度、API实时更新及AIGC检测三大核心难题。

# ---

**亿级论文查重与 AIGC 检测系统技术开发文档 (V2.0)**

## **1\. 系统核心挑战与设计哲学**

* **海量规模：** 10^8（一亿）文档基数，要求极低的指纹存储开销与极高的首层过滤速度。  
* **实时性要求：** API 持续更新要求系统具备“流式入库”能力，确保新文献秒级可见。  
* **深度语义识别：** 针对“洗稿”与 AIGC 生成内容，需突破简单的关键词匹配，引入向量与统计学特征检测。  
* **设计哲学：** **“分层过滤，降维打击”**。通过五层漏斗模型，逐层过滤不相关文档，将昂贵的计算资源集中在最相似的候选集上。

## ---

**2\. 五层过滤漏斗架构**

### **第一层：指纹索引层 (SimHash/LSH) —— 性能屏障**

* **目标：** 快速过滤 90% 以上的不相关文档，处理完全复制或轻微改动。  
* **实现：**  
  * 将 1 亿文档预计算为 64 位 **SimHash** 指纹。  
  * 利用海明距离（Hamming Distance）在 KV 库中检索距离 $k \\le 3$ 的文档。  
* **价值：** 极低成本（1 亿数据约 1GB 内存），秒级响应。

### **第二层：混合检索层 (BM25 \+ Vector) —— 召回保障**

* **倒排索引 (BM25)：** 保证专业术语、核心词汇的精准命中。  
* **向量检索 (Milvus/Qdrant)：** 使用 BERT 模型将段落向量化，解决“同义词替换”与“跨语言抄袭”。  
* **策略：** 两者得分加权合并（Hybrid Search），确保高召回率。

### **第三层：Lambda 实时处理层 —— API 时效性**

* **冷库（存量数据）：** 全量索引存放在分布式存储中。  
* **热库（API 增量）：**  
  * **Kafka** 接入 API 实时流。  
  * **Flink** 进行实时切片、指纹提取与向量化。  
  * **Redis/内存索引** 暂存 24 小时内的新文献。  
* **查询策略：** 同时检索“冷热”两库，结果集自动去重合并。

### **第四层：精排与对齐层 (LCS/Needleman-Wunsch) —— 报告核心**

* **目标：** 确定具体的抄袭位置并标红。  
* **实现：** 对前 100 篇候选文档进行**最长公共子序列 (LCS)** 逐句对齐。  
* **产出：** 左右对照视图、段落重复率、全文相似度。

### **第五层：学术规范与 AIGC 检测层 —— 深度分析**

* **格式检测：** 识别白色字符、公式转图片、隐藏字符等“物理降重”手段。  
* **AIGC 痕迹检测：**  
  * **困惑度 (PPL) 分析：** AI 文本通常具有极低的预测难度。  
  * **爆发性 (Burstiness) 检测：** AI 句式结构过于规整，缺乏人类写作的节奏变化。  
  * **引用溯源：** 自动比对参考文献的真实性，标记编造文献行为。

## ---

**3\. AIGC Trace Detection 专项设计**

为应对 AIGC 论文泛滥，系统在查重报告中集成独立的 AIGC 疑似度评估：

| 检测维度 | 核心指标 | 技术路径 |
| :---- | :---- | :---- |
| **统计特征** | PPL & Burstiness | 基于轻量化语言模型的统计学推算 |
| **语义分类** | Classifier Probability | 基于 RoBERTa-Chinese 的二分类概率判定 |
| **逻辑连贯性** | Logic Entropy | 评估段落间语义跃迁是否符合人类逻辑 |

## ---

**4\. 更新后的技术栈选型**

| 组件分类 | 推荐选型 | 作用说明 |
| :---- | :---- | :---- |
| **核心网关/API** | **FastAPI** | 支撑高并发 API 请求与异步模型调用 |
| **消息队列** | **Apache Kafka** | 削峰填谷，承接 API 实时增量文档 |
| **流计算引擎** | **Apache Flink** | 实时处理新文献的指纹与向量化任务 |
| **向量库** | **Milvus** | 支撑亿级 Embedding 数据的毫秒级检索 |
| **搜索/倒排** | **Elasticsearch** | 负责元数据搜索及关键词 BM25 召回 |
| **任务调度** | **Celery \+ Redis** | 负责后端耗时较长的“生成查重报告”异步任务 |
| **前端交互** | **Vue3 \+ Tailwind** | **极简主义 UI**，展示复杂的查重对照表与雷达图 |

## ---

**5\. 部署与运维策略**

* **冷热分离：** 热数据存放在高性能 NVMe 节点，冷数据存放在分布式文件系统，降低 60% 以上的存储成本。  
* **动态伸缩：** 查重高峰期（毕业季）利用 Kubernetes 快速扩容 Flink 算力节点。  
* **安全合规：** API 接入层增加 ISP 代理检测与频率限制，防止数据库被恶意爬取。

---

**后续迭代重点：**

1. **图表抄袭检测：** 引入视觉特征模型，检测表格与图像的重合度。  
2. **代码查重优化：** 针对计算机专业论文，增加 AST（抽象语法树）比对引擎。