# PRD - 论文评价及检验系统

## 一、产品概述

### 1.1 产品定位
论文评价及检验系统是一款面向高校学生（本科/研究生）的Web应用，旨在提供论文质量检测、智能评价和格式规范化的一站式解决方案。

### 1.2 产品价值
- **提升论文质量**：自动检测错别字、标点符号等基础错误，提高论文专业性
- **智能评价反馈**：基于百炼大模型API，提供多维度学术评价，辅助学生改进论文
- **规范化排版**：自动适配学校/期刊模板，解决格式调整难题

### 1.3 目标用户
- **主要用户**：高校本科生、研究生（学位论文、课程论文、期刊投稿）
- **使用场景**：论文提交前的自查、导师指导前的预检、投稿前的格式规范

---

## 二、用户需求分析

### 2.1 用户痛点
| 痛点 | 场景描述 | 解决方案 |
|------|---------|---------|
| 基础错误频发 | 错别字、标点误用影响论文专业性 | 自动检测+修订标注 |
| 缺乏客观评价 | 不知道论文质量水平，缺乏改进方向 | AI多维度评价报告 |
| 格式调整耗时 | 手动调整格式耗时且易出错 | 模板自动排版 |

### 2.2 核心需求优先级
| 优先级 | 功能 | 理由 |
|--------|------|------|
| P0 | 错别字/标点检查 | 基础刚需，快速见效 |
| P0 | 评价报告生成 | 核心价值，差异化功能 |
| P1 | 模板排版 | 提升效率，但技术复杂度高 |

---

## 三、功能需求详细说明

### 3.1 功能一：论文错别字及标点符号检查

#### 3.1.1 功能描述
对用户上传的Word文档进行错别字、标点符号的智能检测，并生成修订版文档输出。**关键约束**：不改变原文内容，仅以Word修订模式标注建议修改项。

#### 3.1.2 功能流程
```
用户上传Word文档 → 系统解析文档内容 → 执行检测算法
→ 生成修订标注 → 输出修订版Word文档 → 用户下载
```

#### 3.1.3 详细需求

**输入要求**
- 文件格式：.docx
- 文件大小：≤20MB
- 支持内容：纯文本、表格、公式（公式内容跳过检测）

**检测范围**
| 检测类型 | 具体内容 | 示例 |
|---------|---------|------|
| 错别字 | 常见错字、形近字、音近字 | "已经" 误写为 "己经" |
| 标点符号 | 中英文标点混用、标点误用、引号配对 | "，" 与 "," 混用 |
| 空格规范 | 中英文间空格、数字单位间空格 | "Python语言" → "Python 语言" |
| 专业术语 | 学术领域常见术语拼写 | "神经网络" 误写为 "神经网洛" |

**输出规范**
- 使用Word Track Changes（修订模式）标注
- 修订类型区分：
  - 错别字：删除线+插入建议
  - 标点：替换标注
  - 空格：插入标注
- 修订批注：简要说明修改理由（如："疑似错别字"、"标点符号不规范"）

**界面交互**
- 上传区：支持拖拽上传
- 进度显示：实时显示检测进度（百分比）
- 结果预览：网页端简要展示检测问题数量
- 下载按钮：生成修订版文档供下载

#### 3.1.4 技术实现建议
- **Word文档解析**：python-docx库
- **检测算法**：
  - 字典匹配（维护常见错别字库）
  - 正则表达式（标点、空格规则）
  - 可选：集成开源NLP工具（如pycorrector）
- **修订标注**：通过python-docx的Revision功能实现

#### 3.1.5 验收标准
- 检测准确率：≥90%（基于测试语料）
- 误报率：≤15%
- 处理速度：1万字文档≤10秒
- 修订格式正确：可在Word中正常显示修订痕迹

---

### 3.2 功能二：论文内容智能评价

#### 3.2.1 功能描述
调用百炼大模型API对论文内容进行多维度智能评价，生成评价报告并输出为Word/PDF文档。

#### 3.2.2 功能流程
```
用户上传Word文档 → 系统提取文本 → 调用百炼API（传入提示词+论文文本）
→ 接收评价结果 → 格式化生成报告 → 输出Word/PDF → 用户下载
```

#### 3.2.3 详细需求

**评价维度**（基于用户选择）
| 评价维度 | 评价内容 | 输出格式 |
|---------|---------|---------|
| 学术规范性 | 摘要、关键词、引言、结论结构完整性；学术用语规范性 | 评分（0-100）+ 文字说明 |
| 逻辑与创新性 | 论证逻辑严密性、研究创新点、论据充分性 | 评分（0-100）+ 文字说明 |
| 语言质量 | 学术用语准确性、表达清晰度、专业术语使用 | 评分（0-100）+ 文字说明 |
| 文献引用规范性 | 参考文献格式、引用合理性 | 评分（0-100）+ 文字说明 |

**提示词管理**
- 提示词模板由系统预设，内置于代码中（详见"八、百炼大模型API集成说明"章节）
- 用户无需提供或配置提示词，系统自动根据论文内容调用对应的评价提示词
- 提示词模板变量：{{论文标题}}、{{论文内容}}
- 提示词包含4个维度：学术规范性、逻辑与创新性、语言质量、文献引用规范性
- **注**：后期可在管理后台提供提示词优化功能（V2.0+），但用户端不可见

**百炼API集成**
- API配置：支持配置API Key、端点、模型版本
- 调用方式：POST请求，传入提示词+论文文本
- 超时处理：设置60秒超时，超时后提示重试
- 错误处理：API调用失败时展示友好错误信息

**评价报告输出**

**报告结构**
```
1. 报告封面
   - 论文标题
   - 评价时间
   - 系统版本

2. 总体评分
   - 综合评分（四个维度平均分）
   - 雷达图展示各维度得分

3. 分维度评价
   3.1 学术规范性评价
       - 得分：XX分
       - 优点：...
       - 问题：...
       - 改进建议：...

   3.2 逻辑与创新性评价
       - 得分：XX分
       - 优点：...
       - 问题：...
       - 改进建议：...

   3.3 语言质量评价
       - 得分：XX分
       - 优点：...
       - 问题：...
       - 改进建议：...

   3.4 文献引用规范性评价
       - 得分：XX分
       - 优点：...
       - 问题：...
       - 改进建议：...

4. 综合改进建议
   - 优先级排序的改进事项列表
```

**输出格式**
- Word文档（.docx）：带格式，可编辑
- PDF文档（可选）：适合打印存档

**界面交互**
- 上传区：支持拖拽上传
- 评价进度：显示"正在分析..." 状态
- 结果展示：网页端展示简要评分，支持在线查看完整报告
- 下载按钮：下载完整评价报告

#### 3.2.4 技术实现建议
- **API调用**：requests库调用百炼API
- **报告生成**：
  - Word：python-docx生成
  - PDF（可选）：reportlab或wkhtmltopdf
  - 雷达图：matplotlib生成图片后插入文档
- **提示词管理**：提示词模板硬编码在代码中（配置文件或常量），4个评价维度各对应一个提示词模板

#### 3.2.5 验收标准
- API调用成功率：≥95%
- 报告生成时间：≤30秒（不含API响应时间）
- 报告格式正确：Word可正常打开，格式规范
- 评价内容质量：通过人工抽样验证合理性

---

### 3.3 功能三：论文模板排版

#### 3.3.1 功能描述
用户上传论文内容和论文模板（或选择预设模板），系统自动按模板格式进行排版，输出格式化后的Word文档。

#### 3.3.2 功能流程
```
用户上传论文Word → 用户上传/选择模板 → 系统解析模板样式规则
→ 将论文内容映射到模板 → 应用格式 → 输出排版后Word → 用户下载
```

#### 3.3.3 详细需求

**模板来源**
| 模板类型 | 说明 | 操作 |
|---------|------|------|
| 用户上传模板 | 用户上传Word模板文件（.dotx/.docx） | 解析样式：页边距、字体、段落、标题层级等 |
| 预设模板库 | 系统内置常见高校/期刊模板 | 用户选择后直接应用 |

**预设模板库（初期建议）**
- 通用学位论文模板（符合国标GB/T 7713.1-2006）
- 常见高校模板：清华、北大、浙大等（需获取授权）
- 期刊模板：中文核心、SCI期刊（需获取授权）

**模板解析规则**
系统需从模板中提取：
| 样式元素 | 解析内容 |
|---------|---------|
| 页面设置 | 页边距、纸张大小、页眉页脚 |
| 正文格式 | 字体、字号、行距、首行缩进、对齐方式 |
| 标题样式 | 各级标题（1-6级）字体、字号、编号格式 |
| 图表格式 | 图表标题格式、编号规则 |
| 参考文献 | 引用格式（GB/T 7714、APA等） |
| 特殊格式 | 摘要、关键词、公式编号等 |

**智能映射逻辑**
```
1. 结构识别
   - 识别论文中的标题层级（通过字号、加粗、关键词判断）
   - 识别摘要、关键词、引言、正文、结论、参考文献等章节

2. 样式应用
   - 将识别到的各部分应用模板对应样式
   - 自动调整图表编号（如"图1-1"、"表2-3"）
   - 统一参考文献格式

3. 冲突处理
   - 若论文原格式与模板冲突，优先应用模板样式
   - 保留原文内容，仅调整格式
```

**输出规范**
- 输出文件：排版后的Word文档（.docx）
- 文件命名：原文件名_排版版_时间戳.docx
- 格式保证：页码、目录、图表编号自动生成

**界面交互**
```
[上传论文内容]  [上传自定义模板] 或 [选择预设模板▼]
                                    ├─ 通用学位论文模板
                                    ├─ XX大学模板
                                    └─ XX期刊模板

[开始排版] 按钮

排版中... (进度条)

[预览排版结果] [下载Word文档]
```

#### 3.3.4 技术实现建议
- **模板解析**：python-docx解析样式，提取styles、sections信息
- **结构识别**：
  - 基于规则：通过关键词、样式特征识别章节
  - 可选：使用NLP模型增强识别准确率
- **样式应用**：python-docx应用styles到段落和文本
- **模板存储**：预设模板存储在服务器，支持后台管理上传新模板

#### 3.3.5 验收标准
- 模板解析成功率：≥90%（常见格式）
- 排版准确率：≥85%（结构识别正确率）
- 处理速度：1万字文档≤15秒
- 格式保持：输出文档符合模板格式要求

---

## 四、非功能需求

### 4.1 性能需求
| 指标 | 要求 |
|------|------|
| 并发用户 | 支持100并发用户同时使用 |
| 响应时间 | 页面加载≤2秒，文档处理（不含AI）≤15秒 |
| 文件大小限制 | 单文件≤20MB |
| API超时 | 百炼API调用超时设置60秒 |

### 4.2 安全需求
- **数据隐私**：用户上传文档不永久存储，处理完成后24小时自动删除
- **访问控制**：用户仅能访问自己的文档和报告
- **API密钥管理**：百炼API Key加密存储，不暴露给前端
- **文件安全**：上传文件进行病毒扫描，限制文件类型

### 4.3 可用性需求
- **界面友好**：操作流程简洁，不超过3步完成核心任务
- **错误提示**：友好的错误提示信息，避免技术术语
- **帮助文档**：提供操作指南和常见问题解答
- **浏览器兼容**：支持Chrome、Edge、Safari、Firefox最新版本

### 4.4 可扩展性
- **模板库扩展**：支持管理员后台添加新模板
- **提示词管理**：支持后台修改评价提示词
- **API切换**：支持切换不同大模型API（为未来预留接口）

---

## 五、技术架构建议

### 5.1 系统架构图
```
┌─────────────────────────────────────────────────────┐
│                   前端 (Web UI)                      │
│      Vue.js / React + Ant Design / Element UI       │
└─────────────────┬───────────────────────────────────┘
                  │ HTTP/HTTPS
┌─────────────────▼───────────────────────────────────┐
│                 后端 (API Server)                    │
│              Python Flask / FastAPI                  │
│  ┌─────────────┬──────────────┬─────────────────┐  │
│  │ 文档检测模块 │  评价模块     │  排版模块        │  │
│  │  pycorrector│ 百炼API调用   │ python-docx     │  │
│  │ python-docx │ 提示词管理    │ 模板解析        │  │
│  └─────────────┴──────────────┴─────────────────┘  │
└─────────────────┬───────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────┐
│                数据层 & 存储                         │
│  ┌──────────────┬──────────────┬─────────────────┐ │
│  │ MySQL/PG     │ 文件存储      │  Redis缓存      │ │
│  │ 用户/配置    │ OSS/本地      │  会话/任务队列  │ │
│  └──────────────┴──────────────┴─────────────────┘ │
└─────────────────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────┐
│              外部服务                                │
│          阿里云百炼大模型API                         │
└─────────────────────────────────────────────────────┘
```

### 5.2 技术栈推荐

**前端**
- 框架：Vue 3 / React 18
- UI库：Ant Design / Element Plus
- 文件上传：axios + FormData
- 状态管理：Pinia / Redux

**后端**
- 框架：Flask 或 FastAPI（推荐FastAPI，异步性能更好）
- 文档处理：python-docx
- 错误检测：pycorrector / 自定义规则引擎
- 报告生成：python-docx + matplotlib（图表）
- 任务队列：Celery + Redis（处理长时间任务）

**数据库**
- 关系型数据库：MySQL 8.0 / PostgreSQL（存储用户、配置、模板）
- 缓存：Redis（会话、任务状态）

**文件存储**
- 本地存储（初期）
- 对象存储（扩展）：阿里云OSS / AWS S3

**部署**
- 容器化：Docker + Docker Compose
- Web服务器：Nginx（反向代理 + 静态文件）
- WSGI服务器：Gunicorn / Uvicorn

---

## 六、数据流程

### 6.1 功能一：错别字检查
```
1. 用户上传Word文档 → 前端验证（文件类型、大小）
2. 前端发送文件到后端API → 后端保存到临时目录
3. 后端解析Word文档 → 提取文本内容
4. 执行检测算法 → 标记错误位置
5. 应用修订标注 → 生成新Word文档
6. 返回下载链接 → 用户下载文件
7. 定时清理（24小时后删除临时文件）
```

### 6.2 功能二：智能评价
```
1. 用户上传Word文档 → 前端验证
2. 后端解析文档 → 提取纯文本和标题
3. 组装提示词（读取预设的4个维度提示词模板 + 填充论文内容）
4. 调用百炼API → 分别传入4个维度的提示词，获取评价结果（JSON）
5. 解析API返回 → 提取各维度评分和文字说明
6. 生成雷达图 → matplotlib生成PNG图片
7. 生成Word报告 → python-docx组装内容
8. 返回下载链接 → 用户下载报告
```

### 6.3 功能三：模板排版
```
1. 用户上传论文 + 上传模板或选择预设模板
2. 后端解析模板 → 提取样式规则（字体、段落、页边距等）
3. 后端解析论文 → 识别结构（标题、正文、图表、参考文献）
4. 样式映射 → 将论文内容段落应用模板样式
5. 生成新Word文档 → 应用所有格式
6. 返回下载链接 → 用户下载排版后文档
```

---

## 七、交互流程

### 7.1 页面结构
```
首页（Dashboard）
├─ 功能入口卡片
│  ├─ 【错别字检查】
│  ├─ 【智能评价】
│  └─ 【模板排版】
│
├─ 历史记录（最近5条）
└─ 快速帮助

功能页面（以"智能评价"为例）
├─ 上传区（拖拽/点击上传）
├─ 进度展示（处理中...）
├─ 结果展示
│  ├─ 评分雷达图
│  ├─ 简要评价
│  └─ 下载按钮
└─ 返回首页
```

### 7.2 操作流程（以智能评价为例）
```
步骤1：用户点击"智能评价"
       ↓
步骤2：拖拽/点击上传Word文档
       - 前端验证文件格式和大小
       - 显示文件名和大小
       ↓
步骤3：点击"开始评价"按钮
       - 显示进度条 "正在上传..."
       - 上传成功后显示 "正在分析..."（调用API中）
       ↓
步骤4：评价完成，展示结果
       - 显示综合评分（大号字体）
       - 显示雷达图
       - 显示各维度简要评价（可展开详情）
       ↓
步骤5：用户点击"下载完整报告"
       - 下载Word/PDF文档
       - 提示"下载成功，文件将在24小时后自动删除"
```

---

## 八、百炼大模型API集成说明

### 8.1 API配置
```python
# 配置示例（存储在环境变量或配置文件）
BAILIAN_API_KEY = "your_api_key_here"
BAILIAN_ENDPOINT = "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation"
MODEL_VERSION = "qwen-max"  # 或其他模型版本
```

### 8.2 调用示例
```python
import requests
import json

def call_bailian_api(prompt: str, paper_content: str) -> dict:
    """
    调用百炼大模型API
    :param prompt: 提示词模板
    :param paper_content: 论文内容
    :return: API返回的评价结果
    """
    headers = {
        "Authorization": f"Bearer {BAILIAN_API_KEY}",
        "Content-Type": "application/json"
    }

    payload = {
        "model": MODEL_VERSION,
        "input": {
            "prompt": prompt.replace("{{论文内容}}", paper_content)
        },
        "parameters": {
            "max_tokens": 2000,
            "temperature": 0.7
        }
    }

    try:
        response = requests.post(
            BAILIAN_ENDPOINT,
            headers=headers,
            json=payload,
            timeout=60
        )
        response.raise_for_status()
        return response.json()
    except requests.exceptions.RequestException as e:
        # 错误处理
        return {"error": str(e)}
```

### 8.3 提示词模板示例

**学术规范性评价提示词**
```
你是一位资深的学术专家和论文评审专家。请对以下论文的学术规范性进行评价。

论文标题：{{论文标题}}
论文内容：
{{论文内容}}

请从以下角度进行评价：
1. 论文结构完整性：摘要、关键词、引言、研究方法、结果、讨论、结论、参考文献等部分是否齐全
2. 学术用语规范性：是否使用规范的学术术语，避免口语化表达
3. 格式规范性：标题层级、图表标注、公式编号等是否规范

请按以下JSON格式输出：
{
  "score": 85,  // 0-100分
  "strengths": ["优点1", "优点2"],
  "weaknesses": ["问题1", "问题2"],
  "suggestions": ["改进建议1", "改进建议2"]
}
```

**逻辑与创新性评价提示词**
```
你是一位资深的学术专家。请对以下论文的逻辑性和创新性进行评价。

论文标题：{{论文标题}}
论文内容：
{{论文内容}}

请从以下角度进行评价：
1. 论证逻辑：研究问题-方法-结果-结论的逻辑链条是否清晰严密
2. 研究创新性：研究角度、方法或结论是否有新意
3. 论据充分性：数据、案例、文献支撑是否充分

请按以下JSON格式输出：
{
  "score": 80,
  "strengths": ["优点1", "优点2"],
  "weaknesses": ["问题1", "问题2"],
  "suggestions": ["改进建议1", "改进建议2"]
}
```

（其他维度提示词类似结构，此处省略）

---

## 九、迭代规划

### 9.1 MVP版本（V1.0）- 2个月
**目标**：验证核心功能，快速上线

| 功能模块 | 范围 | 优先级 |
|---------|------|--------|
| 错别字检查 | 基础错别字+标点检测，输出修订版Word | P0 |
| 智能评价 | 4个评价维度，生成Word报告 | P0 |
| 用户系统 | 简单的登录注册（可选：先不做，用设备ID区分用户） | P1 |
| 预设模板库 | 1-2个通用模板 | P2 |

**不包含**：模板排版功能（延后到V2.0）

### 9.2 V2.0 - 第3-4个月
**目标**：完善功能，提升体验

| 功能模块 | 范围 |
|---------|------|
| 模板排版 | 支持用户上传模板+5个预设模板 |
| 检测增强 | 增加语法检测、学术用语检测 |
| 报告优化 | 增加PDF输出、报告美化 |
| 用户中心 | 历史记录、使用统计 |

### 9.3 V3.0及后续
- 支持批量处理
- 移动端适配
- 企业版（高校、期刊社）
- 多语言支持（英文论文检测）

---

## 十、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| 百炼API调用失败率高 | 评价功能不可用 | 1. 增加重试机制；2. 提供降级方案（基于规则的简单评价） |
| Word格式解析兼容性差 | 部分文档处理失败 | 1. 提前测试常见格式；2. 提供错误提示，建议用户转换格式 |
| 模板排版准确率低 | 用户不满意 | 1. V1.0不包含此功能，V2.0充分测试后上线；2. 提供手动调整指南 |
| 用户数据泄露 | 信任危机 | 1. 严格的数据安全策略；2. 定期安全审计；3. 隐私协议明确告知 |
| 并发量过大 | 系统卡顿 | 1. 使用任务队列异步处理；2. 增加服务器资源；3. 限流策略 |

---

## 十一、验收标准总览

### 11.1 功能验收
- [ ] 错别字检查：上传Word文档，能正确检测并输出修订版
- [ ] 智能评价：上传论文，能调用API并生成包含4个维度的评价报告
- [ ] 模板排版（V2.0）：上传论文和模板，能输出格式化文档

### 11.2 性能验收
- [ ] 1万字文档错别字检测≤10秒
- [ ] 评价报告生成≤30秒（不含API响应）
- [ ] 支持100并发用户

### 11.3 质量验收
- [ ] 错别字检测准确率≥90%
- [ ] 模板排版准确率≥85%
- [ ] API调用成功率≥95%

### 11.4 安全验收
- [ ] 文件上传安全校验
- [ ] 用户数据24小时自动删除
- [ ] API密钥加密存储

---

## 十二、附录

### 12.1 参考资料
- 百炼大模型API文档：https://help.aliyun.com/zh/dashscope/
- python-docx文档：https://python-docx.readthedocs.io/
- GB/T 7713.1-2006 学位论文编写规则

### 12.2 术语表
| 术语 | 说明 |
|------|------|
| 修订模式 | Word Track Changes功能，标注文档修改痕迹 |
| 百炼 | 阿里云推出的大模型服务平台 |
| python-docx | Python库，用于创建和修改Word文档 |
| 提示词 | Prompt，传递给大模型的指令文本 |

---

**文档版本**：V1.0
**编写日期**：2026-02-09
**编写人**：产品经理
**审核人**：待定
**下次更新**：需求评审后更新
