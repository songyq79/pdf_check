# 模板排版降级处理修复

## 问题描述

用户上传的 Word 文档可能因为格式异常、内部结构损坏等原因导致无法正常读取，报错：
```
"There is no item named 'NULL' in the archive"
```

## 解决方案

实现**降级处理机制**：当文档无法正常读取时，自动尝试提取文本内容并生成简化的格式化文档。

## 实现逻辑

### 处理流程

```
1. 尝试正常格式化
   ↓
   成功 → 返回完整结果
   ↓
   失败 → 判断错误类型
   ↓
2. 如果是文档格式问题
   ↓
   尝试降级处理：
   - 从 ZIP 中提取 document.xml
   - 解析 XML 提取所有文本节点
   - 创建新文档并应用基本样式
   ↓
   成功 → 返回结果 + 警告信息
   ↓
   失败 → 返回友好错误提示
```

### 核心功能

#### 1. 文本提取（`_extract_text_fallback`）
- 将 .docx 文件当作 ZIP 压缩包打开
- 读取 `word/document.xml` 文件
- 解析 XML，提取所有 `<w:t>` 文本节点
- 返回纯文本内容

#### 2. 简化文档生成（`_create_simple_document`）
- 创建新的 Word 文档
- 按段落分割文本并添加到文档
- 尝试应用模板的基本样式
- 保存为新文档

#### 3. 错误处理增强
- 识别文档格式错误（`no item named`、`badzip`）
- 自动触发降级处理
- 返回详细的错误信息和修复建议

## 修改的文件

### 1. `backend/app/core/formatter/format_engine.py`
**新增**：
- `_extract_text_fallback()` - 文本提取函数（约 30 行）
- `_create_simple_document()` - 简化文档生成函数（约 50 行）

**修改**：
- `format_document()` - 添加降级处理逻辑（约 40 行）
- 导入 `zipfile` 和 `xml.etree.ElementTree`

**总代码量**：约 120 行（新增为主）

### 2. `backend/app/workers/formatter_tasks.py`
**修改**：
- `run_formatting()` - 支持 warning 字段，改进错误信息格式（约 15 行）

**总代码量**：约 15 行

## 用户体验改进

### 之前
```
❌ 排版失败
"There is no item named 'NULL' in the archive"
```

### 之后

#### 情况1：降级处理成功
```
⚠️ 排版完成（简化模式）
文档格式异常，已使用简化模式处理（可能丢失部分格式、图片等内容）
✅ 文档已生成
```

#### 情况2：降级处理失败
```
❌ 排版失败
文档格式异常且无法自动修复

建议：
• 请在 Word 中打开文档，选择"另存为"，保存为新的 .docx 文件
• 确保文档能在 Word 中正常打开
• 尝试删除文档中的特殊对象（如嵌入的 OLE 对象、损坏的图片等）
```

## 技术细节

### Word 文档结构
.docx 文件实际上是一个 ZIP 压缩包，包含：
```
document.docx/
├── word/
│   ├── document.xml      ← 主文档内容
│   ├── styles.xml        ← 样式定义
│   ├── numbering.xml     ← 编号定义
│   └── ...
├── _rels/                ← 关系定义
└── [Content_Types].xml   ← 内容类型
```

### XML 命名空间
```python
namespace = {
    'w': 'http://schemas.openxmlformats.org/wordprocessingml/2006/main'
}
```

### 文本节点提取
```xml
<w:p>
  <w:r>
    <w:t>这是文本内容</w:t>
  </w:r>
</w:p>
```

## 优势

1. **自动修复**：无需用户手动处理，系统自动尝试修复
2. **降级优雅**：即使无法完全格式化，也能保留文本内容
3. **信息透明**：明确告知用户使用了降级模式
4. **友好提示**：提供具体的修复建议

## 局限性

1. **格式丢失**：降级模式只保留文本，丢失图片、表格、复杂格式
2. **样式简化**：只能应用基本样式，无法完全还原原文档
3. **不是万能**：如果 ZIP 结构完全损坏，仍然无法处理

## 测试建议

1. **正常文档**：确保不影响正常文档的处理
2. **损坏文档**：测试各种损坏情况（格式错误、ZIP 损坏等）
3. **特殊文档**：测试包含图片、表格、OLE 对象的文档

## 后续优化方向

1. **更智能的文本提取**：保留段落结构、标题层级
2. **图片提取**：尝试提取并保留图片
3. **表格提取**：尝试提取并保留表格结构
4. **格式恢复**：尝试恢复部分格式信息（加粗、斜体等）
