# 模板排版改造需求文档 V2.0

## 改造背景

现有模板排版功能仅支持从内置模板库中选择模板进行排版。用户如果有自己学校或期刊的特定模板格式要求，无法自行添加。V2.0 新增"用户自定义模板上传"功能，支持用户上传 .docx 模板样例，系统自动提取样式配置，经用户预览确认后保存到模板库，后续可直接选用。

---

## 一、核心功能：自定义模板上传（两步确认制）

### 1.1 整体流程

```
用户点击"上传自定义模板"
    ↓
上传 .docx 模板文件 + 填写模板名称
    ↓
后端解析模板，提取样式配置（临时保存，不入库）
    ↓
前端展示"模板预览确认"弹窗，显示提取到的样式信息
    ↓
用户确认 → 正式保存到模板库 → 刷新模板列表
用户取消 → 丢弃临时文件，不保存
```

### 1.2 两步接口设计

| 步骤 | 接口 | 说明 |
|------|------|------|
| 第一步：解析 | `POST /api/v1/formatter/templates/analyze` | 上传模板 → 解析 → 返回样式配置预览（临时保存文件） |
| 第二步：确认 | `POST /api/v1/formatter/templates/confirm` | 用户确认 → 正式保存到模板库 |
| 取消（可选） | `DELETE /api/v1/formatter/templates/temp/{temp_id}` | 用户取消 → 清理临时文件 |

---

## 二、后端改造

### 2.1 新增接口：模板解析预览（第一步）

`POST /api/v1/formatter/templates/analyze`

请求参数（multipart/form-data）：
- `file`: .docx 模板文件（必填）
- `name`: 模板名称（必填）
- `description`: 模板描述（选填）

处理逻辑：
1. 验证文件格式（.docx）
2. 保存到临时目录 `storage/formatter/temp_templates/`
3. 调用 `validate_template()` 验证模板有效性
4. 调用 `extract_config()` 相关方法提取样式配置
5. 生成临时ID（temp_id），用于后续确认或取消
6. 返回提取到的样式配置信息

返回数据结构：
```json
{
  "success": true,
  "temp_id": "abc123",
  "name": "用户填写的模板名称",
  "validation": {
    "valid": true,
    "errors": [],
    "warnings": ["模板中没有示例内容"]
  },
  "config": {
    "page": {
      "width_cm": 21.0,
      "height_cm": 29.7,
      "margin_top_cm": 2.54,
      "margin_bottom_cm": 2.54,
      "margin_left_cm": 3.17,
      "margin_right_cm": 3.17
    },
    "styles": {
      "Normal": {
        "font_name": "宋体",
        "font_size": 12.0,
        "line_spacing": 1.5,
        "first_line_indent": 0.74
      },
      "Heading 1": {
        "font_name": "黑体",
        "font_size": 16.0,
        "bold": true,
        "alignment": "CENTER"
      },
      "Heading 2": {
        "font_name": "黑体",
        "font_size": 14.0,
        "bold": true
      }
    },
    "header": { "text": "XX大学毕业论文", "align": "center" },
    "footer": { "text": "", "page_number": true }
  },
  "style_summary": {
    "正文": "宋体 12pt，1.5倍行距，首行缩进0.74cm",
    "一级标题": "黑体 16pt，加粗，居中",
    "二级标题": "黑体 14pt，加粗",
    "三级标题": "黑体 12pt，加粗",
    "页面设置": "A4纸，上下边距2.54cm，左右边距3.17cm"
  }
}
```

### 2.2 新增接口：模板确认保存（第二步）

`POST /api/v1/formatter/templates/confirm`

请求参数（JSON）：
```json
{
  "temp_id": "abc123",
  "name": "我的学校论文模板",
  "description": "XX大学2026年毕业论文格式要求"
}
```

处理逻辑：
1. 根据 temp_id 找到临时文件
2. 调用现有的 `save_template()` 方法正式保存
3. 清理临时文件
4. 返回保存后的模板信息

返回数据：
```json
{
  "success": true,
  "template_id": "a1b2c3d4e5f6g7h8",
  "name": "我的学校论文模板",
  "message": "模板保存成功，可在模板列表中选择使用"
}
```

### 2.3 新增接口：取消/清理临时文件

`DELETE /api/v1/formatter/templates/temp/{temp_id}`

处理逻辑：
1. 删除临时目录中对应的文件
2. 返回成功

### 2.4 现有接口修改

#### 模板列表接口（无需修改）
`GET /api/v1/formatter/templates` 已支持返回自定义模板（category="custom"），无需改动。

#### 删除模板接口（新增）
`DELETE /api/v1/formatter/templates/{template_id}`

目前后端 `TemplateManager.delete_template()` 方法已实现，但没有暴露 API 路由。需要新增路由。

### 2.5 后端文件改动

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/app/api/v1/formatter.py` | 修改 | 新增 analyze、confirm、delete 三个路由；补 `import shutil` |
| `backend/app/core/formatter/template_manager.py` | 小改 | 新增 `extract_config_from_path()` 方法（从文件路径直接提取配置，不需要先入库）；新增 `generate_style_summary()` 方法（生成人类可读的样式摘要） |

---

## 三、前端改造

### 3.1 模板排版页面（Formatting.vue）

在模板列表卡片中新增：

1. "上传自定义模板"按钮（放在模板列表底部或卡片头部右侧）
2. 点击后弹出"上传模板"对话框（el-dialog）

#### 上传模板对话框（第一步）

内容：
- 模板名称输入框（el-input，必填）
- 模板描述输入框（el-input，选填）
- 文件上传区域（el-upload，仅 .docx）
- "解析模板"按钮

点击"解析模板"后：
- 调用 `POST /templates/analyze` 接口
- 显示加载状态
- 成功后切换到"预览确认"视图

#### 预览确认视图（第二步，同一个对话框内切换）

展示内容（使用 el-descriptions 组件）：
- 页面设置：纸张大小、页边距
- 正文样式：字体、字号、行距、首行缩进
- 一级标题样式：字体、字号、是否加粗、对齐方式
- 二级标题样式：同上
- 三级标题样式：同上
- 页眉/页脚信息（如果有）
- 验证警告信息（如果有，用 el-alert 显示）

底部按钮：
- "确认保存"（el-button type="success"）→ 调用 confirm 接口 → 成功后关闭弹窗、刷新模板列表、自动选中新模板
- "取消"（el-button）→ 调用 delete temp 接口 → 关闭弹窗

### 3.2 模板列表中的自定义模板

自定义模板在列表中显示时：
- 标签显示为"自定义"（el-tag type="info"）
- 右侧增加"删除"小按钮（仅自定义模板可删除，内置模板不显示）
- 删除前二次确认（ElMessageBox.confirm）

### 3.3 前端 API（formatting.js）

新增方法：
```javascript
// 解析模板（第一步）
analyzeTemplate(formData) { ... }

// 确认保存模板（第二步）
confirmTemplate(data) { ... }

// 取消/清理临时模板
cancelTemplate(tempId) { ... }

// 删除已保存的自定义模板
deleteTemplate(templateId) { ... }
```

### 3.4 前端文件改动

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `frontend/src/views/Formatting.vue` | 修改 | 新增上传按钮、上传对话框、预览确认视图、自定义模板删除按钮 |
| `frontend/src/api/formatting.js` | 修改 | 新增 4 个 API 方法 |

---

## 四、临时文件管理

### 4.1 临时文件存储

- 目录：`backend/storage/formatter/temp_templates/`
- 文件命名：`{temp_id}.docx`
- 配套 JSON：`{temp_id}_config.json`（缓存解析结果，confirm 时直接读取）

### 4.2 临时文件清理

- 用户点击"确认保存"后自动清理
- 用户点击"取消"后自动清理
- 定时清理：超过 1 小时未确认的临时文件自动删除（可选，后续实现）

---

## 五、不改动的部分

- 格式化引擎（format_engine.py）不动
- 样式应用器（style_applicator.py）不动
- 结构分析器（structure_analyzer.py）不动
- 现有内置模板不动
- 评价功能不动
- 错别字检查功能不动
- Celery 任务逻辑不动

---

## 六、涉及文件清单

| 文件路径 | 改动类型 |
|---------|---------|
| `backend/app/api/v1/formatter.py` | 修改（新增3个路由，补import） |
| `backend/app/core/formatter/template_manager.py` | 小改（新增2个方法） |
| `frontend/src/views/Formatting.vue` | 修改（新增上传对话框、预览确认、删除按钮） |
| `frontend/src/api/formatting.js` | 修改（新增4个API方法） |

---

## 七、用户操作流程示例

### 场景：用户上传学校论文模板

1. 进入"模板排版"页面
2. 点击"上传自定义模板"按钮
3. 弹窗中填写模板名称："XX大学2026毕业论文模板"
4. 上传学校提供的模板 .docx 文件
5. 点击"解析模板"
6. 系统显示提取到的样式信息：
   - 页面：A4，上下2.54cm，左右3.17cm
   - 正文：宋体 12pt，1.5倍行距
   - 一级标题：黑体 16pt，加粗，居中
   - ...
7. 用户确认无误，点击"确认保存"
8. 模板保存成功，自动出现在模板列表中
9. 用户选择该模板，上传论文，一键排版

###V2.1迭代
1. 目录行间距无法调整 style_applicator.py 的 _apply_paragraph_style 里，样式映射没有 toc 类型，目录段落会走 body 样式，不会单独处理行距。

2. 二级/三级标题缩进无法调整 _apply_numbering 里只给标题加了编号文字，但没有设置 left_indent。_apply_format 里虽然有 left_indent 逻辑，但模板提取的样式里这个值通常是 None，所以不生效。

3. 参考文献引用序号无法变上标 完全没有实现。代码里没有任何扫描正文中 [1]、[2] 这类引用标注并转为上标的逻辑。

4. 图、表、公式格式无法调整 structure_analyzer.py 里虽然定义了 FIGURE、TABLE、FORMULA 类型，但 style_applicator.py 的 style_map 里没有这三个类型的映射，识别出来也不会应用任何样式。

5. 参考文献国标格式无法调整 只是把参考文献段落识别出来应用了 references 样式（字体/行距），没有任何对参考文献条目格式（作者、年份、期刊名顺序）的重排逻辑。
