# 论文查重模块需求文档 V2.0

> 版本：v2.0.0 · 更新日期：2026-03-20 · 适用系统：论文评价检验系统

---

## 一、项目概述

### 1.1 背景与目标

当前系统已具备智能评价、错别字检查、模板排版三大功能，新增查重模块旨在：

- 为用户提供文本相似度检测，标注疑似重复片段
- 输出结构化查重报告，支持来源溯源与在线高亮展示
- 采用**可扩展架构**，初期使用开源算法（V1），同步支持百炼大模型语义增强（V2）
- 对上层 UI 和报告页面**零改动**地完成底层引擎切换
- 检测速度优先：万字论文 V1 ≤ 5秒，V2 ≤ 20秒

### 1.2 核心设计原则

> **查重引擎与展示层完全解耦。** 所有算法实现封装在 `BaseChecker` 接口之后，接口返回固定数据结构。升级引擎时，前端代码无需任何修改。

### 1.3 技术路线

| 阶段 | 引擎 | 说明 |
|------|------|------|
| **V1（当前）** | 开源算法（SimHash + TF-IDF） | 零成本，快速上线，结果仅供参考 |
| **V2（当前）** | 混合引擎（开源 + 百炼大模型） | 语义增强，识别改写型抄袭 |
| **V3（规划中）** | 商业数据库（万方 / 维普） | 对接正规学术库，结果权威可信 |

V1 和 V2 同时实现，通过环境变量 `PLAGIARISM_ENGINE` 切换，无需重启。

---

## 二、技术栈

### 2.1 后端（Python，适配现有项目）

| 功能 | 技术 | 说明 |
|------|------|------|
| 文档解析 | `python-docx`（已有）、`pdfplumber` | .docx / .pdf / .txt |
| 分句清洗 | 标点符号切句 + 短句合并 | 替代滑动窗口，减少片段数量 |
| 指纹算法 | `simhash` + numpy 矩阵运算 | 批量计算，比逐句循环快 10-50 倍 |
| 语义相似度 | `scikit-learn` TF-IDF + 余弦相似度 | 辅助识别改写型抄袭 |
| 简繁转换 | `opencc-python-reimplemented` | 统一文本格式 |
| 语义增强 | 阿里云百炼 API（已有 openai SDK） | V2 灰色地带判断 |
| 异步队列 | Celery（已有） | 复用现有队列，新增 plagiarism 队列 |
| 结果缓存 | Redis（已有） | 文本 MD5 → 缓存 24 小时 |

### 2.2 新增依赖

```
# requirements.txt 追加
scikit-learn>=1.4.0
pdfplumber>=0.10.0
simhash>=2.1.0
opencc-python-reimplemented>=0.1.7
```

---

## 三、系统架构

### 3.1 架构分层

```
┌─────────────────────────────────────────┐
│         API 路由层（plagiarism.py）       │
│   接收请求 · 鉴权 · 文件解析             │
├─────────────────────────────────────────┤
│         CheckerContext（单例）           │
│   策略持有者，读取环境变量选择引擎        │
├─────────────────────────────────────────┤
│         BaseChecker（抽象基类）          │
│   定义统一契约：check(text) → CheckResult│
├──────────────────┬──────────────────────┤
│  OpenSourceChecker│   HybridChecker      │
│  V1：SimHash+TF-IDF│  V2：开源+百炼并发  │
└──────────────────┴──────────────────────┘
```

### 3.2 V1 开源引擎流程

```
文档解析（docx/pdf/txt）
    ↓
文本清洗（去标点、简繁统一）
    ↓
语义分句（按句号/问号/感叹号切分，合并短句到30-100字）
    ↓
numpy 批量 SimHash 指纹计算
    ↓
LSH 近似最近邻搜索（跳过明显不相似片段）
    ↓
TF-IDF 余弦相似度辅助验证
    ↓
阈值 ≥0.75 标记重复
    ↓
构建 CheckResult（≤5秒）
```

### 3.3 V2 混合引擎流程

```
V1 粗筛结果
    ├── similarity ≥ 0.75 → 直接标记，不调百炼
    ├── similarity 0.5~0.75 → 灰色地带
    │     ↓
    │   预筛选（去除 <20字片段、纯数字/公式/参考文献格式）
    │     ↓
    │   同一来源已确认 ≥3 个片段 → 剩余直接标记，不再调 API
    │     ↓
    │   Semaphore(6) 动态并发控制
    │     ↓
    │   第一级（qwen-turbo，stream=True）：流式接收，收到 YES/NO 立即停止
    │     └── YES → 第二级（qwen-plus，max_tokens=300）：深度确认
    │   单片段超时 8秒 → 跳过，不阻塞
    └── similarity < 0.5 → 丢弃

整体百炼调用超过 15秒 → 自动降级返回 V1 结果
百炼不可用 → 静默降级，engine 字段标记为 opensource-fallback
```

**两阶段返回策略（提升用户感知速度）：**

1. V1 粗筛完成后立即返回初步结果给前端展示（status = `partial`）
2. V2 百炼补充判断完成后推送最终结果（status = `done`）
3. 前端轮询时检测 status，`partial` 时先渲染 V1 结果，`done` 时更新为最终结果

### 3.4 速度目标

| 场景 | 优化前 | 优化后 |
|------|------|------|
| V1 万字论文 | ~15秒 | ≤ 5秒 |
| V2 万字论文（20个灰色片段） | ~60秒 | ≤ 8秒 |
| V2 万字论文（5个灰色片段） | ~20秒 | ≤ 3秒 |
| 用户感知首屏结果 | 等全部完成 | V1完成即展示（~5秒） |
| 重复文件（缓存命中） | ~15秒 | < 1秒 |

---

## 四、新增文件清单

```
backend/app/
├── api/v1/
│   └── plagiarism.py                    # HTTP 接口（新增）
├── core/plagiarism/
│   ├── __init__.py
│   ├── base_checker.py                  # 抽象基类 + CheckResult 数据结构
│   ├── checker_context.py               # 策略持有者（单例，读取环境变量）
│   ├── text_parser.py                   # 文档解析 + 语义分句 + 清洗
│   ├── prompts.py                       # 三级提示词（V2用）
│   └── engines/
│       ├── __init__.py
│       ├── opensource_checker.py        # V1：SimHash + TF-IDF + numpy批处理
│       └── hybrid_checker.py            # V2：开源 + 百炼并发调用
└── workers/
    └── plagiarism_tasks.py              # Celery 任务（新增，queue="plagiarism"）

frontend/src/
├── views/
│   └── Plagiarism.vue                   # 查重页面（上传 + 报告展示）
├── store/modules/
│   └── plagiarism.js                    # Pinia store
└── api/
    └── plagiarism.js                    # 接口封装
```

**原有文件最小改动（仅追加）：**

| 文件 | 改动 |
|------|------|
| `backend/app/api/v1/router.py` | 末尾追加 `include_router(plagiarism.router)` |
| `frontend/src/router/index.js` | 追加 `/plagiarism` 路由 |
| `frontend/src/views/Home.vue` | 追加查重功能卡片 + getRecordTypeName/Tag 映射 |
| `frontend/src/views/History.vue` | 追加 plagiarism 类型映射 + handleView 分支 + 筛选选项 |
| `backend/requirements.txt` | 追加4个新依赖 |
| `backend/.env` | 追加 `PLAGIARISM_ENGINE=hybrid` |

---

## 五、接口规范

### 5.1 统一返回数据结构（所有引擎固定格式）

```json
{
  "task_id": "uuid-v4",
  "engine": "hybrid-bailian",
  "created_at": "2026-03-20T10:00:00Z",
  "summary": {
    "total_chars": 12000,
    "dup_chars": 2400,
    "rate": 20.0,
    "risk_level": "medium"
  },
  "highlights": [
    {
      "id": 1,
      "text": "疑似重复的句子内容...",
      "start_pos": 320,
      "end_pos": 380,
      "similarity": 0.87,
      "source_id": "src-001",
      "ai_judged": true,
      "verdict": "改写抄袭"
    }
  ],
  "sources": [
    {
      "id": "src-001",
      "title": "来源文献标题",
      "author": "作者名",
      "year": 2022,
      "url": null
    }
  ]
}
```

### 5.2 HTTP 接口

```
POST /api/v1/plagiarism/upload
  参数：file（.docx/.txt）、paper_type（选填）
  返回：{ task_id, status: "pending", estimated_seconds }

GET  /api/v1/plagiarism/status/{task_id}
  返回：{ task_id, status: "pending/processing/done/failed", progress: 0-100 }

GET  /api/v1/plagiarism/report/{task_id}
  返回：完整 CheckResult JSON
```

> 暂不支持 PDF（V1 阶段），避免中文扫描版 PDF 解析坑，V2 阶段再加。

---

## 六、提示词设计（V2）

### 6.1 第一级：快速判断（qwen-turbo，stream=True）

流式接收，收到 YES/NO 立即停止，不等后续 token。批量判断多个片段，一次调用处理 5-8 个。

```
你是学术论文查重专家。判断以下各片段是否与对应参考片段构成学术不端。

{{#each segments}}
【片段{{index}}】待检测："{{text}}" | 参考："{{reference}}"
{{/each}}

判断规则：
- 文字重复度 > 70% → 抄袭
- 核心论点相同但句式完全重写 → 抄袭
- 通用学术表述（"研究表明""本文提出"）→ 正常
- 纯数字、公式、参考文献格式 → 正常

输出格式（严格按此，不要多余文字）：
1: YES/NO
2: YES/NO
...
```

### 6.2 第二级：深度确认（qwen-plus，max_tokens=300）

```
你是学术诚信审查专家。

待审查片段："{{segment}}"
疑似来源："{{reference}}"
所属章节：{{section}}

从字面相似度、语义相似度、改写程度、引用规范四个维度分析。

输出格式（严格 JSON）：
{
  "is_plagiarism": true/false,
  "confidence": 0-100,
  "rewrite_detected": true/false,
  "verdict": "直接抄袭/改写抄袭/正常引用/正常",
  "reason": "一句话说明"
}
```

---

## 七、前端页面设计

### 7.1 Plagiarism.vue 页面结构

```
上传区（未检测时）
  └── el-upload 拖拽上传（.docx / .txt）
  └── 论文类型选择（本科/硕士/博士/期刊，选填）
  └── 开始检测按钮

检测中
  └── 进度条 + 预计剩余时间
  └── V1 完成后立即展示初步结果（标注"初步结果，AI增强检测中..."）
  └── V2 完成后自动刷新为最终结果

报告区（检测完成后）
  ├── 顶部：文件名 + 引擎标签 + 检测时间
  ├── 演示提示栏（V1时显示"结果仅供参考"）
  ├── 概览：环形图（SVG动画，重复率）+ 4格数据卡片
  │     （总字符数 / 重复字符数 / 疑似段落数 / 检出来源数）
  └── 详情 Tabs
       ├── 原文高亮（三色：红≥80% / 橙60-80% / 绿<60%）
       ├── 片段列表（表格，含 AI 标记角标）
       └── 来源详情（文献列表，含相似度百分比）
```

### 7.2 高亮颜色规范

| 颜色 | 含义 | 相似度范围 |
|------|------|------|
| 🔴 红色 | 高相似度 / 直接抄袭 | ≥ 80% |
| 🟡 橙色 | 中相似度 / 改写抄袭 | 60~80% |
| 🟢 绿色 | 低相似度 / 疑似 | < 60% |

`ai_judged: true` 的片段显示「AI」角标。

### 7.3 联动交互

点击来源文献 → 左侧原文滚动到对应片段并高亮闪烁（单向联动，实现简单可靠）。

### 7.4 历史记录接入

检测完成后写入 historyStore：

```js
historyStore.addRecord({
  type: 'plagiarism',
  timestamp: Date.now(),
  result: {
    file_name: '文件名.docx',
    rate: 20.0,
    risk_level: 'medium',
    engine: 'hybrid-bailian',
    task_id: 'xxx'
  }
})
```

首页"最近记录"和历史记录页自动显示，与其他三个模块完全一致。

---

## 八、结果缓存策略

- 文本内容 MD5 → Redis key `plagiarism:cache:{md5}`
- 缓存有效期 24 小时
- 相同文件秒返回，不重复消耗计算资源

---

## 九、Celery 配置

新增 `plagiarism` 队列，Celery 启动命令追加：

```bash
celery -A app.workers.celery_app worker -P solo \
  -Q default,proofread,evaluation,formatter,plagiarism --loglevel=info
```

---

## 十、环境变量

`.env` 追加：

```
PLAGIARISM_ENGINE=hybrid   # opensource / hybrid
```

---

## 十一、不改动的部分

- 智能评价模块不动
- 错别字检查模块不动
- 模板排版模块不动
- 历史记录 store 逻辑不动（只追加 type 映射）
- AI 客户端（ai_client.py）不动
- Celery 配置文件不动

---

## 十二、开发顺序

1. `text_parser.py` + `base_checker.py`（基础层）
2. `opensource_checker.py`（V1 核心算法 + numpy 批处理）
3. `plagiarism_tasks.py` + `plagiarism.py`（接口层 + 缓存）
4. `hybrid_checker.py` + `prompts.py`（V2 百炼并发层）
5. 前端 `Plagiarism.vue`（上传 + 报告页）
6. 首页加卡片 + 路由 + 历史记录接入

---

## 十三、错误码

| 错误码 | 含义 | 处理 |
|------|------|------|
| `PARSE_FAILED` | 文档解析失败 | 检查文件格式 |
| `TEXT_TOO_SHORT` | 文本少于 200 字 | 提示上传完整论文 |
| `ENGINE_TIMEOUT` | 引擎超时 | 自动重试，最多 3 次 |
| `BAILIAN_UNAVAILABLE` | 百炼不可用 | 自动降级至 V1 |
| `FILE_TOO_LARGE` | 文件超过 20MB | 返回 413 |
