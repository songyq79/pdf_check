# 平台功能路线 & 维普对标结论

> 本文档记录论文助手平台的功能升级路线、维普 API 集成结论、数据壁垒判断与待办。
> 团队后续决策优先读本文 + [CLAUDE.md](CLAUDE.md) / [CLAUDE-MAP.md](CLAUDE-MAP.md)。

## 一、核心判断:算法在手,数据是壁垒

平台的 LLM + RAG + 多语言语义重排管线是差异化优势。**反复指向同一结论:中文期刊/文献数据是唯一真壁垒**,而非算法。因此战略是:

- **买数据,不买整功能**:用维普的原始数据 API 喂自己的管线(保差异化、更便宜),只在维普有独家数据处才直接买其复合功能。
- **守住底线**:不代写(不接维普"研学创作-全文")、不造假(无数据的板块留占位,不让 LLM 编造)。

## 二、维普 API 对标结论(41 个接口)

| 类别 | 接口 | 处理 |
|---|---|---|
| 原始数据检索 | 文献检索(高级/分领域)、向量检索、文献详情 | ✅ 已接(喂选题/综述/查重) |
| 期刊数据 | 期刊查询、期刊详情、JCR影响因子 | ✅ 已接(投稿选刊富化) |
| 引文工具 | 文献引用格式、题录格式 | ⏸️ 可选(gbt7714 已够) |
| AI 复合 | 投稿预测分析报告 | ⏸️ 维普独家,V-3 待做 |
| AI 复合 | 智能选题/调研综述/投稿选刊 | ❌ 不转卖(用数据+自有管线更优) |
| 代写 | 研学创作(全文) | ❌ **坚决不接**(违背学术诚信) |
| 下载类 | 文献下载/专利下载 | ❌ 版权风险,慎用 |

**关键限制**:本套是维普"学术 API 开放平台"(文献/AI 工具),**不含查重检测接口**。真·查重率报告需单独申请维普**检测产品**或知网。

## 三、已落地功能(均真机验证,在 master)

### 维普集成
- **V-0** 维普文献检索源 `external/cqvip.py` 接入 aggregator → 选题评估/推荐、文献综述、英文查重兜底**共享真实中文文献**(`5cef392`)
- **V-1** 投稿选刊「相似文献去向」:文献检索聚合论文发表期刊 → 真实期刊推荐(`abebfe6`)
- **中文查重真实来源**:可疑段落查维普 → 真实相似论文出处,替代 LLM 编造 + 诚实标注(`7b3071e`)
- **V-2** 投稿选刊富化:点开看真实 IF/分区/审稿周期/ISSN,缓存进 journals 表越用越全(`e621b79`)

### 功能新增/升级
- 选题推荐(双 Tab:推荐/评测,LLM+真实文献,非造假期刊指南)
- 投稿选刊(主题语义匹配 → 升级为相似文献去向)
- 文献综述:长文/短文模式 + 输出语言(中/英)
- 文献检索语义重排去噪(多语言模型 paraphrase-multilingual-MiniLM-L12-v2)
- 整篇写作辅助(上传 docx 异步逐段四维分析)
- 粘贴文本查重(文件/粘贴二选一)
- 学生工作台论文进度路线图(真数据点亮 8 阶段)

### B端
- 机构控制台 / 学生工作台侧边栏外壳(InstLayout/InstShell)、登录 Tab 化、入口打通

### 计费
- FEATURE_COSTS 单一真相源 + 全功能消耗透明化 + 各功能提交前 CostHint

## 四、数据壁垒路线

| 库 | 状态 | 路线 |
|---|---|---|
| 维普 | ✅ 已接开放平台(文献/期刊) | 已用;查重检测需另购 |
| 知网 | ❌ 无自助 API | 等 B端 签约学校,借学校授权走正规通道(私有化部署/官方接口),**别碰灰产 API** |
| 万方 | ⚠️ 有限 | 备选 |

## 五、待办 / 可选

- **V-3 投稿预测分析报告**(维普独家,8.8/次)— 贴接口41文档即可做
- **B端 查重/AIGC 学生端管控**(默认导师发起/学生受限,防逐段降重规避;含粘贴查重入口)
- **AIGC 检测**(等维普/知网检测产品,走接口不自研,避免误判风险)
- 引文格式权威化、向量检索(锦上添花,低优先)

## 六、运维提醒

1. **APP_ENV**:本地读 `.env`,线上 `APP_ENV=production` 读 `.env.production`——**两个都要配** `VIP_API_KEY` 等。
2. **新增队列**:Celery worker 需监听 `topic_eval,lit_review,experiment_eval,writing_assist`(及原有队列)。
3. **模型缓存**:`backend/models/`(多语言重排模型 ~470MB)已 gitignore,服务器首次用自动下载或预置;连不上 HF 时重排优雅降级。
4. **密钥**:真实 key 只放 `.env`/`.env.production`(gitignore),**绝不入库**;`.env.example` 仅占位。
5. **远程**:GitHub `origin/master` 已推;gitee 由本地 `git push gitee master`。
